# AGENTS.md — QorAct (Grok Build / Grok Bot)

You are building the **authorship plane**, not truth and not observation.

## Job

Keep `build_qoract` / `verify_qoract` honest. Reference-and-bind only.

## Plane lock

- Do not speak humanity, eligibility, ban, `poep_enabled`, heat, or “the AI clutched.”
- Do not open a capture card. Do not dual-open DShow.
- Do not wrap dest `qortroller-truth` from Qoresence MCP.
- Do not mint a FROZEN-v1 primitive or run PV-CI `--generate --confirm-governance`.
- Do not write chain (`CHAIN_SUBMISSION_PAUSED`).
- Do not add a wallet, IoID, token, or second identity.

## Consume only

- Qoresence `session-recap-1`, Foundry `hdmi_clip_*` + sidecars, actuator receipts (Aperture/Bind/License/Arm).
- Optional QorTroller KAS / PoSP / PORT-CERT as **hashes**.
- Clock Notary seal after `I am the gamer`. Forbidden signers: `bridge`, `operator`, `qoresence`, `qortroller`, `qoract`.

## Closed enums

Verdict: `HUMAN_AUTHORED` | `AGENT_AUTHORED` | `MIXED` | `UNVERIFIABLE`
Rollup: `COMPLETE` | `PARTIAL_SURFACES` | `UNVERIFIABLE`
Media v0 kinds: `pixels` | `speech`

## Fail-closed laws

1. Outcome-agent is `undeployed` until something injects HID.
2. Empty laptop HID (DualSense on PS5) ⇒ outcome `UNVERIFIABLE` unless KAS/PoSP ref or bodied+IVC.
3. Arm/intent without a file ⇒ no pixels span.
4. `path=hold` / empty text ⇒ no speech span.
5. Confirm-path chat is still `AGENT_AUTHORED` speech.
6. Ignore producer `status`. Recompute `clock_commitment`.
7. `live: true` is forbidden on this candidate.
8. Human HOLD beats every PASS. ConWan30 is sovereign. Do not push `main` without explicit “push”.

Q-ACT-2 Recap-door reader is fail-open; do not merge without Con.

## Grok Build

```text
cd QorAct
grok -p "Run python -m unittest tests/test_qoract.py and stop if red."
```

Do not expand kinds to edit/publish/narrative without a new Decision block from Con.
